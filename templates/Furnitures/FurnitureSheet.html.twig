{% extends 'base.html.twig' %}

{# {% block title %}
	{{ title }}
{% endblock %} #}

{% block body %}
	<div class="row">
		<div class="card bg-light mb-3" style="max-width: 20rem;">
			<div class="card-header">{{ furniture.name }}
				<p>{{ furniture.type }}</p>
			</div>
			<div class="card-body">
				<p class="card-text"></p>
				{% for f in furniture.materials %}
					<a class="card-text" href="{{ path('material_sheet', { 'id' : f.id }) }}">
						{{ f.name }}
					</a>
				{% endfor %}
			</div>
		</div>
	</div>

{% endblock %}
